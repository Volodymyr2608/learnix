/** Course badges shown inline before collapsing the rest into a "+N more" badge. */
export const VISIBLE_COURSES = 2;

/** Max characters of a course title shown in a badge before truncating. */
export const MAX_TITLE_LENGTH = 20;
